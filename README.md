# Example app for GraphCMS - Svelte - Houdini 

## Getting started

After cloning, run the following commands:

```zsh
npm install

npm run dev
```

The app will be available at `http://localhost:3000`.
